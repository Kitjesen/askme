# Normalize text in Git while preserving each platform's normal checkout EOL.
# Existing files are intentionally not mass-renormalized by this change.
* text=auto

*.sh text eol=lf

*.bat text eol=crlf
*.cmd text eol=crlf
