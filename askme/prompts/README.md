# askme.prompts

Code-level prompt registry.

This package stores Python prompt-template definitions used by product code.
Runtime prompt assets such as `SOUL.md` belong in the repository-root
`prompts/` directory.

Use this package when code needs a named `PromptTemplate`. Do not put markdown
persona files, customer prompt drafts, or generated prompt outputs here.

