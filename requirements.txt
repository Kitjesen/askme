# All runtime dependencies are declared in pyproject.toml [project.dependencies].
# This file exists so `pip install -r requirements.txt` installs the editable
# package from the project metadata.
-e .
